export interface dataSetsVM{
    label ?: string;
    type ?: string;
    data ?: number[];
    backgroundColor ?: string[];
    borderColor ?: string[];
    borderWidth ?: number
    tension ?: number
    fill ?: boolean
}