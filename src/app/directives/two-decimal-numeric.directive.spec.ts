import { TwoDecimalNumericDirective } from './two-decimal-numeric.directive';

describe('TwoDecimalNumericDirective', () => {
  it('should create an instance', () => {
    const directive = new TwoDecimalNumericDirective();
    expect(directive).toBeTruthy();
  });
});
