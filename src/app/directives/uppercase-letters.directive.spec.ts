import { UppercaseLettersDirective } from './uppercase-letters.directive';

describe('UppercaseLettersDirective', () => {
  it('should create an instance', () => {
    const directive = new UppercaseLettersDirective();
    expect(directive).toBeTruthy();
  });
});
