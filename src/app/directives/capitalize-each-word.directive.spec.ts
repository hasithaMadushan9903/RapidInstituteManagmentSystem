import { CapitalizeEachWordDirective } from './capitalize-each-word.directive';

describe('CapitalizeEachWordDirective', () => {
  it('should create an instance', () => {
    const directive = new CapitalizeEachWordDirective();
    expect(directive).toBeTruthy();
  });
});
